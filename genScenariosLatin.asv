function [r, vol] = genScenariosLatin(nu, vol, t, nSamples, beta0, beta1, beta2, alpha, dt)

%

nDays = t/dt
xi = lhsnorm(zeros(nDays,1), eye(nDays), nSamples)';

for i = 1:nDays
    rTemp = nu*t + sqrt(vol).*xi(i,:)*sqrt(t);
    vol = beta0 + beta1*vol + beta2/t*(rTemp - alpha*t).^2;
end





